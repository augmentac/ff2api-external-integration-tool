streamlit>=1.28.0
pandas>=1.5.0
openpyxl>=3.1.0
requests>=2.28.0
cryptography>=3.4.8
numpy>=1.24.0
python-dateutil>=2.8.2
beautifulsoup4>=4.12.0
aiohttp>=3.8.0
asyncio-throttle>=1.0.2

# Cloud-compatible HTTP and API libraries
httpx>=0.25.0
aiofiles>=23.0.0
tenacity>=8.0.0
fake-useragent>=1.4.0
plotly>=5.0.0

# Phase 2 Enhancement Dependencies
# Proxy Integration and Advanced Request Handling
aiohttp-proxy>=0.1.2
aiohttp-socks>=0.8.0
pysocks>=1.7.1
urllib3>=1.26.18

# Advanced Browser Fingerprinting
user-agents>=2.2.0
python-user-agents>=2.3.0

# CloudFlare Bypass and Enhanced Security
tls-client>=0.2.0
httpx-socks>=0.7.0

# JSON and Data Processing
ujson>=5.8.0
orjson>=3.9.0

# Advanced Logging and Monitoring
colorlog>=6.7.0
structlog>=23.0.0

# Time and Date Utilities
pytz>=2023.3
tzlocal>=5.0.1

# Enhanced Error Handling
backoff>=2.2.1
retrying>=1.3.4

# Removed cloud-incompatible dependencies:
# - selenium (requires browser binaries)
# - webdriver-manager (requires browser binaries)  
# - playwright (requires browser binaries)
# - requests-html (requires browser binaries)
# - cloudscraper (requires browser binaries)
# - undetected-chromedriver (requires Chrome installation)
# - curl-cffi (requires system-level curl compilation)

# Phase 2 Features:
# - Advanced browser fingerprinting with device profile rotation
# - Proxy integration with IP rotation and geolocation matching
# - CloudFlare bypass capabilities
# - Enhanced session management with connection pooling
# - Human behavior simulation with realistic timing patterns
# - SSL/TLS fingerprinting for better request authenticity
# - Comprehensive diagnostic and monitoring capabilities 